---
description: Show auto-capture status — turn count, consolidation threshold, and whether consolidation is due.
allowed-tools: [Bash]
---

```bash
node ${CLAUDE_PLUGIN_ROOT}/scripts/memory_auto_capture.js status
```
